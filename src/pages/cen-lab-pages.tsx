// NOTE: For a pure Pages Router project, rename this file to `cen-lab.tsx`.
// It is named `cen-lab-pages.tsx` here to avoid conflicts with the App Router example.

import CenLabBossTimer from "@/components/CenLabBossTimer";

export default function CenLabPage() {
  return <CenLabBossTimer />;
}
